package com.fit.se.message.domain;

public enum MessageType {
    TEXT,
    SYSTEM,
    DEFAULT,
    CALL,
    USER_JOIN,
    REPLY,
    CONVERSATION_NAME_CHANGE,
    CONVERSATION_AVATAR_CHANGE,
    POLL_RESULT
}